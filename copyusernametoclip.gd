extends Label


# Called when the node enters the scene tree for the first time.
#func _input(event):
#	if event is InputEventMouseButton && event.pressed && event.button_index == 1:
#		DisplayServer.clipboard_set(text)
#		print("copy'ed to clipboard")

